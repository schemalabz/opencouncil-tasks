# Development Dockerfile with hot reloading support
FROM node:20-alpine

# Set the working directory
WORKDIR /app

# Copy package files first for better caching
COPY package.json package-lock.json ./

# Install all dependencies (including dev dependencies)
# Using cache mount for faster builds - this persists npm cache across builds
# See: https://docs.docker.com/build/cache/
# No python3, same as the production image. The fuse core is TypeScript now and
# runs on the Node that is already here. Dev and production must not differ in
# what the fusion can execute, which is the whole reason this line existed.
RUN --mount=type=cache,target=/root/.npm \
    npm ci && \
    apk add --no-cache ffmpeg

# Expose the port the app runs on
EXPOSE ${PORT}

# Ensure ffprobe is discoverable by the app
ENV FFPROBE_PATH=/usr/bin/ffprobe

# Start the development server with hot reloading
CMD ["npm", "run", "dev"] 